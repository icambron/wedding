%a.btn{href: new_family_path}
  %i.icon-plus
  %span New Family

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Name
      %th Responded?
      %th Visited?
      %th Attending
      %th Not Attending (so far)
      %th Edit
  %tbody
    - @families.each do |family|
      %tr
        %td= family.name
        %td
          %i{class: family.responded ? "icon-ok" : "icon-remove"}
        %td
          %i{class: family.visited ? "icon-ok" : "icon-remove"}
        %td
          = family.people.to_a.select{|m| m.attending}.map{|m| m.name}.join(", ")
        %td
          = family.people.to_a.select{|m| !m.attending}.map{|m| m.name}.join(", ")
        %td
          %a.btn{href: edit_family_path(family.id)}
            %i.icon-pencil
            %span Edit
